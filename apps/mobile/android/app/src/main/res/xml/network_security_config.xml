<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext HTTP is permitted ONLY to the local dev backend (wrangler dev,
     reached via the Android emulator host alias 10.0.2.2). All other traffic
     must be HTTPS — production sync targets the deployed Worker over TLS. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
